{
  "name": "chrome-new-tab-bookmarks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist release",
    "build": "vite build",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "package": "node ./scripts/package.mjs",
    "release": "npm run typecheck && npm run build && npm run package"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.40",
    "typescript": "^5.9.3",
    "vite": "^7.3.2"
  }
}
